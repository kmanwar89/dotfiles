6/17/2016

touchpad.py is just a python implementation of a script to disable the touchpad.  Doesn't dynamically check if the state is currently enabled or disabled (easy to fix) and only disables, doesn't toggle